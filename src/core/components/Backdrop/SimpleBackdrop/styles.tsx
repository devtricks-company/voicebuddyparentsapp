import styled from 'styled-components';
import {Text} from '../../Text';

export const StyledTitle = styled(Text)`
  position: absolute;
  text-align: center;
  left: 0;
  right: 0;
  z-index: -1;
`;
