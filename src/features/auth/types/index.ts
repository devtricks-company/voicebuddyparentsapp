export type UserProfileParams = {
  first_name: string
  last_name: string
  kind: { name: string; id: string }
}

export type ServerProfile = {
  first_name: string
  last_name: string
  kind: { name: string; id: string }
}
